import 'dart:ui';

const backgroundColor = Color.fromRGBO(5, 10, 18, 1); // Very dark, near black
const textColor = Color.fromRGBO(220, 230, 240, 1); // Off-white for contrast
const appBarColor = Color.fromRGBO(10, 18, 25, 1); // Slightly darker
const webAppBarColor = Color.fromRGBO(12, 20, 28, 1); // Even slightly darker
const messageColor = Color.fromRGBO(10, 60, 65, 1); // Deep blue-green
const senderMessageColor = Color.fromRGBO(8, 35, 40, 1); // Deep muted blue
const tabColor = Color.fromRGBO(0, 120, 150, 1); //  Deep rich blue
const searchBarColor = Color.fromRGBO(18, 25, 30, 1); // Dark gray-blue
const dividerColor = Color.fromRGBO(15, 23, 30, 1); //  Deeper gray-blue
const chatBarMessage = Color.fromRGBO(15, 20, 25, 1); // Darker still
const mobileChatBoxColor = Color.fromRGBO(12, 20, 28, 1); // Adjust if desired
